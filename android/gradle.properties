# Project-wide Gradle settings.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.enableJetifier=true
kotlin.code.style=official

# API Configuration
# Pour utiliser le backend Render (production), commenter la ligne suivante:
# API_BASE_URL=http://127.0.0.1:8080/api/v1
# Le fallback dans build.gradle.kts utilisera automatiquement Render

# Pour utiliser un backend local, décommenter et modifier:
# API_BASE_URL=http://127.0.0.1:8080/api/v1

# URL de référence pour le backend Render
API_BASE_URL_PRODUCTION=https://allinconnect-back-1.onrender.com/api/v1
